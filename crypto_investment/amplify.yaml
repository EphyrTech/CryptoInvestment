version: 1
applications:
  - appRoot: crypto_investment/amplify
    frontend:
      phases:
        preBuild:
          commands:
            - yarn install
        build:
          commands:
            - yarn build
